# Environment / secrets
.env
.env.*
!.env.example
*.key
*.pem

# Go build output + local database
/cella
/cella.exe
*.db
*.db-journal
*.db-wal

# Node (if any tooling/build is added)
node_modules/
npm-debug.log*
yarn-error.log*

# Build / output
/dist/
/build/
/coverage/
*.log

# SBOM / reports (regenerated in CI)
/sbom.json
/sbom.xml

# Local-only reference material (contributor runbooks, live vote data — never publish)
/reference/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local delegate roster (real addresses — keep it out of the repo)
/roster.json
